# Research Assistant - Requirements File
# 
# This project uses uv as the primary package manager.
# For uv users: Run `uv sync` or `uv pip install -e .` instead.
# For pip users: This file is provided for compatibility.
#
# To install with pip:
#   pip install -r requirements.txt
#
# To install with uv:
#   uv pip install -r requirements.txt
#
# Note: The canonical source of dependencies is pyproject.toml.
# This file mirrors those dependencies for pip compatibility.

# Core LangChain dependencies
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.10
langchain-openai>=0.0.5

# LLM Providers
openai>=1.10.0
# anthropic>=0.8.0  # Optional: uncomment if needed

# HTTP Client (for OpenRouter and custom API calls)
httpx>=0.25.0
requests>=2.31.0
urllib3>=2.0.0

# Vector stores and embeddings
chromadb>=0.4.0
# faiss-cpu>=1.7.4              # Optional
# sentence-transformers>=2.2.2  # Optional

# Web search, scraping, and browsing
beautifulsoup4>=4.12.0
lxml>=5.0.0
readability-lxml>=0.8.1
duckduckgo-search>=4.0.0

# Data processing
pandas>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.0
tiktoken>=0.5.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Development
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0

# Logging and monitoring
loguru>=0.7.0
